version: '3.8'

services:
  cetus-bot:
    build: .
    container_name: cetus-rebalance-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
